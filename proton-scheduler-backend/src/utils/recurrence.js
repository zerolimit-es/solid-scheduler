/**
 * Recurrence Utilities — Re-exports from @zerolimit/solid-pod-sync
 */
export {
  parseRRule,
  buildRRule,
  generateOccurrences,
  describeRecurrence,
  getNextOccurrence,
  isSameOccurrence,
  generateSeriesId,
  expandRecurringEvent,
  RECURRENCE_PRESETS,
} from '@zerolimit/packages/solid-pod-sync/core';

import {
  parseRRule,
  buildRRule,
  generateOccurrences,
  describeRecurrence,
  getNextOccurrence,
  isSameOccurrence,
  generateSeriesId,
  expandRecurringEvent,
  RECURRENCE_PRESETS,
} from '@zerolimit/packages/solid-pod-sync/core';

export default {
  parseRRule,
  buildRRule,
  generateOccurrences,
  describeRecurrence,
  getNextOccurrence,
  isSameOccurrence,
  generateSeriesId,
  expandRecurringEvent,
  RECURRENCE_PRESETS,
};
